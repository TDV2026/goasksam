# Launched Aug 2026: the site is public and indexable. The pre-launch rule that
# blocked everything but /privacy while the curtain was up has been removed.
# /onebox stays crew-gated and carries its own noindex, so it is not listed here.
User-agent: *
Allow: /
