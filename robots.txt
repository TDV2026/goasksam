# CURTAIN (pre-launch): block indexing until launch day.
# The privacy policy is intentionally allowed: it must stay publicly reachable
# (Google OAuth verification + user access) while the app itself is curtained.
# Delete this Disallow (or replace with an Allow) in the single curtain-removal commit.
User-agent: *
Allow: /privacy
Allow: /privacy.html
Disallow: /
